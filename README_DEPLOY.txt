This package is Netlify-ready. Settings:
- Build command: (empty)
- Publish directory: .
- Add environment variable GROQ_API_KEY in Netlify settings.

Functions in netlify/functions/api-chat.js will proxy to Groq API.
